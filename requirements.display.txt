# ─────────────────────────────────────────────────────────────────────────────
# requirements.display.txt — Display Agent packages
# opencv-python: GUI window support (not the headless build)
# ─────────────────────────────────────────────────────────────────────────────

fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# OpenCV with GUI support — required for imshow on RPi5 X11
# Same package is used on Mac (imshow is not called in headless mode)
opencv-python==4.10.0.84

numpy==1.26.4
pydantic==2.9.2
requests==2.32.3
