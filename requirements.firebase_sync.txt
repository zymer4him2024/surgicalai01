# ─────────────────────────────────────────────────────────────────────────────
# requirements.firebase_sync.txt — Firebase Sync Agent 전용
# ─────────────────────────────────────────────────────────────────────────────

fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
httpx==0.27.2

# Firebase Admin SDK (Firestore + Storage)
# 자격증명 없으면 시뮬레이션 모드로 자동 전환됨
firebase-admin==6.5.0

# 환경변수 (.env 로드)
python-dotenv==1.0.1

# 이미지 처리 (스냅샷 노출 보정)
opencv-python-headless==4.10.0.84
numpy==1.26.4

# Pydantic
pydantic==2.9.2

# 테스트
pytest==8.3.4
pytest-asyncio==0.24.0
